import React from 'react';
import ReactDOM from 'react-dom';
import App from './App';

// Render's the app.js and put it in the div with the id "root"
ReactDOM.render( < App / > , document.getElementById('root'));
